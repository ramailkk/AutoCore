"""Trivial file #2 in the mixed-risk scenario — a comment-only tweak,
should score zero."""

# TODO: consider adding a CHANGELOG entry for this module someday.


def add_note(notes: list[str], text: str) -> None:
    notes.append(text)
