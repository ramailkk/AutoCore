"""Greeting helper. Scenario: NONE — clean, correct code with nothing to
flag. Opening a PR that adds/touches this file should classify as "none"."""


def greet(name: str) -> str:
    return f"Hello, {name}!"
